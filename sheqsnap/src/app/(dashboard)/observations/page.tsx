import ObservationsClient from "./ObservationsClient";

export const metadata = {
  title: "Behaviour Observations | SHEQSnap",
};

export default function ObservationsPage() {
  return <ObservationsClient />;
}
